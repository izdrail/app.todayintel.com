{{--
|--------------------------------------------------------------------------
| Header Title
|--------------------------------------------------------------------------
|
| This template is shown in the header on every page.
|
--}}

<a title="{{ config('waterhole.forum.name') }}" href="{{ route('waterhole.home') }}" class="header-title nowrap h3">
    {{ config('waterhole.forum.name') }}
</a>
